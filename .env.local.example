# ============================================
# Appnexu — Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# cp .env.local.example .env.local

# ------ Authentication (Auth.js v5 / NextAuth v5) ------
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here"
AUTH_TRUST_HOST=true
# IMPORTANT: Auth.js v5 uses AUTH_URL (NOT NEXTAUTH_URL)
# Required for Next.js 16 compatibility on Vercel
AUTH_URL="https://your-domain.com"

# ------ Database (PostgreSQL / Neon) ------
# Local dev:  postgresql://user:pass@localhost:5432/anyapp_dev
# Production: postgresql://user:pass@ep-xxx.neon.tech/anyapp?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/anyapp_dev"
# For Supabase: use pooler URL (port 6543) for DATABASE_URL, direct (port 5432) for DIRECT_URL
# DIRECT_URL="postgresql://user:password@db.xxxx.supabase.co:5432/postgres"

# ------ Stripe (optional for local dev) ------
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_PUBLISHABLE_KEY="pk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."
# STRIPE_PRO_PRICE_ID="price_..."
# STRIPE_AGENCY_PRICE_ID="price_..."

# ------ App Domain (for custom domain detection) ------
# NEXT_PUBLIC_APP_DOMAIN="your-domain.com"

# ------ Queue — Upstash QStash (optional for local dev) ------
# Jobs run inline without these in dev mode
# QSTASH_TOKEN="qst_..."
# QSTASH_CURRENT_SIGNING_KEY="sig_..."
# QSTASH_NEXT_SIGNING_KEY="sig_..."
JOB_SECRET="dev-job-secret"
